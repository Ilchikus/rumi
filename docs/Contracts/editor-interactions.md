---
status: accepted
area: editor
owner: web
created: "2026-07-18"
updated: "2026-08-20"
---
# Editor Interactions

The proven Rumi editor is the functional baseline for the official editor. Its ProseMirror schema,
commands, plugins, NodeViews, and interaction styling are migrated together so the editing model is
preserved. Browser/platform adapters replace its Electron and direct-filesystem calls; Markdown
remains the durable representation and workspace operations remain server-owned.

The repository-level [Markdown syntax reference](../../MARKDOWN.md) defines the GFM baseline,
Rumi-specific source extensions, and read-time compatibility rules used by this contract.

Inline code and the pending inline-code input state use theme-aware Rose text on the neutral code
surface. Fenced code and Mermaid source retain theme-aware syntax-highlighted text colors.

## Behavior Map

| User problem | Action | Expected result |
| --- | --- | --- |
| Choose an application appearance | Open Settings and choose Auto, Light, or Dark | Auto is the default and follows the operating system, including live system changes. The explicit browser-local preference is scoped to the current workspace and applies before React renders. Light keeps the existing white canvas and Neutral 50 sidebar. Dark uses Neutral 800 for the canvas and Neutral 700 for the sidebar, address bar, code blocks, and other subtle surfaces. Product accents use Sky 500 with Sky 400 hover states. Markdown highlights use Yellow 500 at 30% opacity in Light and 20% in Dark. Syntax, Mermaid, database, overlay, dialog, menu, and notification surfaces resolve from the same theme. |
| Configure appearance, writing assistance, startup, and uploads | Open Settings from the Gear action above Trash and change a theme, editor, startup, or upload control | `/settings` is an editor-like system page with the same title, content container, and fixed-width address bar as an ordinary page. Settings are not divided into named sections: each setting is one name/compact-description/options row. File-format options alone use two checkbox columns and show lowercase dotted extensions, including `.svg`. Native checkboxes use the Sky 500 accent. Valid workspace-setting changes save automatically without a Save button, and serialized persistence prevents an older request from replacing a newer change. Theme is browser-local as described above. Misspelling highlighting defaults off; inline replacements and emoji suggestions default on. The shadcn-style **Editor toolbar** dropdown offers `Floating`, `Top`, `Bottom`, and `None`: Floating keeps the compact selection toolbar, Top fixes the full toolbar below the workspace header, Bottom fixes it above the viewport's bottom safe-area inset, and None hides it. Both full positions align to the editor column. All editor preferences update the live editor without remounting or losing undo history. The browser-local startup dropdown defaults to `Last visited`, can instead open `Home`, and is scoped to the current workspace and browser rather than shared through workspace configuration. Upload size defaults to 50 MB; blank means unlimited and zero disables uploads. Clearing every format checkbox also disables uploads. Changes apply to subsequent uploads. |
| Start or reopen a workspace | Open an authenticated Rumi instance at `/`, including after closing the prior browser session | The sidebar, header, configured startup page, sidebar expansion, and layout-affecting editor settings hydrate from bounded browser-local state, then reconcile to fresh server state. By default that page is the last visited page and its last successfully loaded or saved content; choosing `Home` keeps the cached workspace root as the startup page instead. The startup-page preference applies only to a cold visit of `/`. Every non-home URL, including a bookmarked page, remains authoritative: Rumi may hydrate its cached shell and sidebar, but it does not hydrate or redirect to the persisted last page and resolves the requested route against the fresh server tree. The last visited target survives sessions, but page scroll positions do not: a newly opened Rumi session starts its page at the top. Cached workspace data never renders before authentication and never replaces an unsaved draft. Interaction-only views and data do not block the initial home page. |
| Navigate with browser history or share an open item | Open a page, folder, database, record, or Trash; scroll it; navigate elsewhere and revisit it; use Back/Forward or load its URL directly; copy the open page's URL from File actions or Shift-Mod-C; copy its relative path from File actions or Shift-Mod-P | The address changes to a URL-safe workspace slug. Copy URL combines that canonical application route with the current origin, while Copy relative path uses the canonical workspace-root-relative path, beginning with `/` and including its Markdown filename where applicable. Both menu and keyboard entry points write through the same callbacks, and shortcuts are inactive when no workspace page is open. Back/Forward restores that history entry's exact editor-canvas position, while ordinary in-app revisits restore the page's latest in-memory position during the current application session. Deep links survive refresh, and the persistent application layout does not remount. Scroll state is not written to persistent browser storage and is not restored after closing and reopening Rumi. |
| Move an open page to Trash | Choose Move to Trash from its menu | The reversible move happens immediately without confirmation, and Rumi opens the latest surviving page from the in-app visit history. |
| Review or remove a trashed page | Open Trash, select a viewable item, then Restore or Delete | Trashed pages remain addressable in a read-only page view even though they are absent from the sidebar. Restore is reversible; Delete always asks for confirmation that the operation is permanent and irreversible. |
| Rename or split the open-page title without leaving the editor | Click the filename text or the unused part of its row, edit it, then blur or press Enter | Text clicks preserve the exact caret position; row clicks place it at the end. Blur renames, while Enter keeps the title before the caret and moves the remainder into a new first content block. Mod-Z reverses either operation as one action, restores the original title, and reopens it with the caret at the end. The extension stays hidden, pending edits save first, and Escape cancels. |
| Create or rename a spaced workspace item from the sidebar | Open a folder or database action menu, choose an available New action, type a name containing ordinary spaces, and submit it | Folder menus can create a page, folder, or database beneath that folder. Database menus can create pages only. The controlled input retains in-progress spaces. Portable unsafe characters are still replaced, while leading and trailing whitespace is trimmed only at submit. Sidebar and inline-title naming accept the same ordinary spaces. Primary-modifier click or Enter on **New Page** skips the inline naming row, expands its parent, projects the canonical page returned by create into the current sidebar tree, opens it, and selects its complete default title for replacement; a deferred authoritative tree refresh reconciles that optimistic node. Ordinary activation keeps the inline naming flow. A pointer-opened sidebar context menu focuses its first enabled action; Up/Down wraps across enabled actions, Enter activates the focused action once, and Escape returns focus to the originating row control when it still exists. |
| Create structure without leaving the keyboard | Type Markdown prefixes or use `/` | Headings, lists, tasks, quotes, code, Mermaid, dividers, tables, bookmarks, database references, images, and files become semantic blocks. A paragraph-start triple-backtick fence stays literal while its optional language is typed, then Enter commits it: a bare fence becomes plain code, an info string such as `php` becomes a language-tagged code block, and `mermaid` becomes editable Mermaid source. The same text stays literal before Enter, mid-paragraph, and inside an existing code block, and Mod-Z restores the complete literal fence. Typing `[]`, `[x]`, `- []`, `- [x]`, `-[]`, or `-[x]` at the start of a line and then pressing Space creates an unchecked or checked task item; the spaced GFM form `- [ ]` is accepted too. The bracket sequence remains literal until that trailing Space. The slash menu follows the active line through native editor scrolling, prefers the space below it, flips above when needed, and scrolls internally when neither side can fit it in full. |
| Read prose and lists with consistent rhythm | View adjacent paragraph, bullet, numbered, and task blocks | Top-level paragraphs use 5 pixels of vertical spacing on both sides. Flat bullet, numbered, and task rows use 2 pixels on both sides, a shared 20-pixel decoration column, and an exact 8-pixel gap between the decoration and text. Paragraph text and top-level list decorations start flush with the editor content edge instead of inheriting an extra left inset. Bullet glyphs are 32 pixels; bullets, ordered markers, unchecked boxes, and nesting guides use semantic muted/input colors, while checked boxes retain the Sky 500 active state. Ordered markers use tabular numerals, align left, restart at `1` after a non-numbered block, and continue their outer sequence across nested items. Checkboxes sit 2 pixels below the row top for optical text alignment. Every nested flat-list row shows one muted vertical guide per indentation level, centered on the corresponding decoration column. Soft and hard line breaks inside one block remain compact. |
| Enter symbols and emoji without leaving the keyboard | Type a supported ASCII combination, or type `:` at a prose boundary and continue with an emoji query | Inline combinations such as `->`, `<->`, `<=`, and `<=>` become `→`, `↔`, `≤`, and `⇔` in live ProseMirror state and later save as literal UTF-8 Markdown. Chained arrow matches remain one logical undo action. A valid `:smile` query opens Rumi's dependency-free, caret-anchored emoji selector with `😄` focused; Enter replaces the exact query, while Escape closes the selector and leaves it literal. Code, Mermaid, inline code, URLs, timestamps, paste, load, and programmatic changes do not trigger either feature. |
| Continue naturally around special blocks | Press Enter, Backspace/Delete, Shift-Enter, Tab, Shift-Tab, an arrow key, or Mod-Enter | Lists split without discarding an empty current item: Enter preserves that bullet, number, or task row and creates another row of the same kind. Backspace or Delete on an empty formatted text block, including a heading or list item, resets it to a plain blank paragraph while preserving the caret on the same line. On an already-plain blank paragraph, the same key removes that paragraph and moves the caret to the previous text block's end. Typing an opening backtick starts a pending inline-code session tied to the continuing input actions, with no clock or character limit. A closing backtick in that session creates the durable code mark and leaves the caret outside it. Caret movement, navigation, blur, paste, or another structural action cancels the session and leaves ordinary backticks, which save as escaped Markdown literals; Mod-Z immediately after closure restores both literal backticks and removes the code mark. Inline-code boundary navigation is currently browser-native; consistent code-side affinity is deferred. Mermaid source is ordinary ProseMirror code content, so it accepts a normal text caret, literal paste, tabs, Shift-Enter newlines, and the editor's single undo history. Database embeds and dividers each expose leading and trailing structural caret positions on their own row: the database's leading caret appears at its top-left corner and its trailing caret at its bottom-right corner. Arrow navigation crosses between those positions without highlighting the whole block, and Delete from the leading or trailing position removes that block. Removing a trailing blank paragraph leaves the matching trailing caret; a file embed remains the sole caretless inactive boundary. Mod-Enter adds a paragraph after the current block and Shift-Mod-Enter adds before; inside code or Mermaid, Mod-Enter retains the code-exit behavior. One LF is a stable visible soft break within a paragraph, a blank line is the paragraph/block boundary, and explicit hard breaks remain compact and inline. Tables move between cells. |
| Paste from plain, rich, and Rumi sources | Use normal paste or the platform paste-as-plain-text shortcut | Normal paste prefers an exact Rumi structural flavor, then supported semantic HTML, then text. A complete SVG source document pasted normally outside block or inline code uploads as a collision-safe `pasted-svg.svg` asset and inserts the same image block as a file upload; runtime validation rejects malformed or active SVG before publication. SVG source stays literal inside code and for paste-as-plain-text. Pasting an HTTP(S), `www.`, or unambiguous bare-domain destination over highlighted text preserves that label and creates a link; at a plain caret it inserts the clipboard value as both label and portable destination. Scheme-less web destinations render and open through HTTPS without rewriting their stored Markdown. Pasting a workspace path over highlighted text also creates a link, while a workspace path at a plain caret remains ordinary text. Normal paste replacing one complete inline-code span preserves its code mark. Paste-as-plain-text never promotes a URL/domain or preserves source formatting: it replaces the highlighted range or inserts literal text. Native text selection copies only the selected characters and inline marks, so text copied from a task does not gain `- [ ]`; explicit block selection retains list and task syntax. An explicit block selection is the paste replacement range even when ProseMirror's native selection is only a stale cursor or one selected node: every selected block is removed, pasted content starts at the first selected block, and both block and native node highlights clear to an available text cursor afterward. Google Sheets ranges become table blocks through rich HTML. Adjacent paragraph blocks use one LF in plain text and compact line containers in portable HTML so normal and plain-text Sheets paste do not create blank rows; exact Rumi structure remains in the private flavor. Semantic lists from Google Docs and similar editors become flat Rumi list blocks with their available family and nesting; block wrappers inside rich table cells are flattened into inline cell content without breaking the table. A Google Docs paragraph run becomes code only when every line retains both the portable code font and background; ordinary monospace prose remains prose. Docs' default visual link underline remains presentation rather than becoming an explicit Rumi underline mark, and whitespace Docs folds into the anchor boundary returns outside the link when it separates surrounding text instead of becoming part of the link label. When an intermediary strips private metadata but leaves an unambiguous Mermaid grammar or database configuration run, Rumi promotes that text back to its semantic block; metadata absent from both returned HTML and text cannot be recovered. Paste-as-plain-text ignores HTML and treats Markdown punctuation literally, while preserving single LFs as soft breaks and blank lines as paragraph boundaries; code-block paste remains literal. |
| Connect knowledge quickly | Type `@`, paste a URL or path over selected text, press Mod-Shift-K, use the link toolbar action, right-click a link, or deliberately follow one | An explicit portable Markdown link is inserted. Plain source text that merely matches `http://`, `https://`, `www.`, an email, or a domain remains plain after load; link state comes only from explicit source syntax such as `[label](destination)`. Normal URL/domain paste and editor link actions create that syntax on save, while paste-as-plain-text does not. Mod-K remains the application-wide search shortcut; adding Shift opens the same link action as the editor toolbar without colliding with search. With highlighted unlinked text, that action opens the URL editor; with highlighted linked text it toggles the mark off without changing the logical selection or editor-canvas scroll offset. With a caret in linked text it opens the URL editor with the complete destination focused and selected. Copy, Open, Unlink, and Apply are accessible icon actions in the same row as the destination input. A successful copy replaces its icon with a checkmark for 500 milliseconds before the editor closes. Typing a workspace destination shows one normal-weight, baseline-aligned inline suggestion over a bottom-border-only path input rather than a dropdown. The suggested path is muted except for the exact matching substring, which uses foreground text. Root path-prefix matches outrank filename-only matches; Up/Down cycles candidates, Tab accepts without applying, and Enter accepts the active canonical path before applying it. Left/Right retain normal input-caret movement, and a search query is never stored in place of the accepted destination. Page, folder, and database titles are searchable; folders and databases store their existing `.index.md` and `.db.md` companion paths, and their direct title or basename matches outrank descendant paths that only share the directory prefix. Applying an empty destination removes the link, so Backspace followed by Enter unlinks directly without moving the viewport. The same editor opens from secondary mousedown/right-click and the formatting toolbar; hovering a link never opens link UI. Its complete surface is excluded from block area-selection capture, so primary pointer actions remain inside that editor session and execute only their context action, including when the session began from right-click. Internal destinations are URI-decoded and resolved from the containing document or workspace root, then the matched node receives its current application slug; application slugs are never written over canonical file paths. Mentions, custom-label internal anchors, and external anchors all use one semibold link representation with one derived `link_marker` component; only its internal/external variant, glyph, destination, and anchor semantics differ. The internal variant selects the page, folder, or database glyph, while the external variant uses the original Phosphor Globe. Every link marker uses the same regular Phosphor weight and `1.2em` size, so all glyphs share one visual weight and scale with their paragraph or heading. The marker creates the same outside-to-anchor caret boundary for either variant: moving right enters anchor editing, moving left exits, and Backspace on the anchor side removes only the destination while preserving its text. The custom outside-boundary caret uses the containing textblock's computed font size rather than editor-level `em` inheritance, so it retains the block's normal height when an icon-led link begins the line. A complete anchor selection also highlights its marker with the platform text-selection colors. The marker is an explicit navigation affordance: ordinary click opens an external destination in a focused new tab and an internal destination in the current window, Mod-click opens either kind in a focused new tab, and Shift-click opens either kind in the current window. Modified anchor mousedown is consumed before ProseMirror selection handling, and icon activation follows the destination stored on its marker instead of depending on DOM-position mapping. A plain primary click on anchor text places the caret without navigating; Mod-click and Shift-click use the same explicit new-tab and current-window targets. The marker occupies one real ProseMirror position, uses a zero-height layout box, and is omitted from Markdown and portable clipboard serialization, leaving only `[label](destination)` in durable source. Applying a link leaves the caret after the complete rendered link. Link text keeps a text cursor without an activation modifier and changes hover color with a pointer cursor while Mod or Shift is held; the marker always uses a pointer cursor. Native secondary-click selection and browser context menus are disabled throughout the application while Rumi-owned context actions continue to work. |
| Format selected text | Triple-click a row, press Mod-A, use the selection toolbar, or use formatting shortcuts | Triple-click selects only the clicked hard-break-separated row, including inside code blocks. It never expands that row to the whole multi-row block. With an active editor caret, the first Mod-A selects the cursor's whole block without opening its context menu, so typing can replace that block; a second Mod-A selects every block. When no text-editing caret is active, Mod-A falls back directly to selecting every block on the current page instead of selecting browser page text. Bold, italic, underline, strike, code, the single default yellow highlight, and links are applied as Markdown-backed marks. When blocks are explicitly selected, toolbar actions and formatting shortcuts apply to every selected block, including non-contiguous selections, without formatting unselected blocks between them. A mixed selection becomes uniformly marked on the first toggle and uniformly unmarked on the next. Toggling the checkbox of a selected task gives every selected task the toggled checked state; selected non-task blocks are unchanged. The Top and Bottom editor toolbar modes expose the same full bar, separating block placement, undo/redo, block conversion/media, inline formatting, and final block deletion into five groups divided by vertical borders. The groups distribute across the bar's full available width while controls inside each group remain compact. Their block conversions share the handle context menu's transaction behavior but omit Mermaid, Table, and Divider, which remain available from the handle menu. Placement, movement, history, and replacement controls show their shortcuts in the button title. Toolbar and hotkey undo/redo invoke the same ProseMirror history commands. Inline actions preserve and format the highlighted text, while delete invokes the same canonical whole-block deletion as the handle menu. Floating retains only the compact inline controls, stays directly below the highlighted text in document space, and scrolls away with that text rather than following the viewport. Applying a floating formatting action, including Link Apply, collapses its text or explicit block highlight and closes the toolbar. Escape does the same, including from the link editor. Top, Bottom, and None preserve the active editor selection after formatting. |
| Reorganize document structure | Use each block's handle, Command-D, modifiers, area selection, or drag | Command-D duplicates the explicit block selection, or the cursor's whole active block when there is no block selection; the duplicate becomes the active block selection. Command-click on macOS and Control-click on Windows/Linux toggles any block handle into or out of the explicit selection, including non-contiguous sets. Holding the same primary modifier while drawing a marquee toggles every block in that area against the existing selection, allowing multiple separated marquee ranges and removal of an already-selected range. One or many blocks can also be deleted, reordered, and, for list items, indented or outdented. Shift-Up/Down retains native ProseMirror behavior for inline selections; for explicit block selections it moves an anchored range head, adding a block when moving away from the anchor and removing the previous edge when reversing direction. A one-block selection begins growing in the requested direction. Command-click or a primary-modifier marquee starts a new active range without discarding earlier selected groups, and subsequent Shift movement changes only that range. Shift-Mod-Up/Down likewise retains native inline behavior but extends a block selection to the matching document edge. A held marquee remains attached to its original document point while the page scrolls, continues extending over newly revealed blocks, and proportionally auto-scrolls inside the top and bottom 100-pixel viewport zones. Every block-delete entry point removes the selected block nodes completely, clears the explicit selection, leaves the editor inactive with no visible text or structural caret, and does not request any viewport movement. Deleting the entire document retains the schema's required empty paragraph without activating its caret; clicking back into the editor establishes the next cursor normally. A top-level list drop crosses into its first indent after 30% of the editor width; a drop beneath an already-indented item aligns with that item until it crosses 20% of the target item's own width, then nests one level deeper. Every list item is independently addressable. Any click outside the editable block area clears both its block highlight and NodeSelection after the clicked control completes its own behavior. |
| Change a thought's role | Press Mod-/, right-click a block handle, type `/`, or select one or more blocks and type a type name after explicitly opening the menu | Mod-/ toggles the same menu as right-clicking a block handle. It uses an existing block selection or selects the cursor's current block when needed, without expanding that selection; pressing Mod-/ again closes the menu. Selecting blocks through handles, Mod-A, or area selection remains quiet and does not open the menu. Right-clicking a selected block's handle preserves the complete selection; activating a handle outside the selection first resets selection to only that block so stale blocks cannot receive the action. The selection-opened menu remains outside the content column with an 8-pixel gap before the content start. The first printable key reaches search even during the handle's drag-safe focus handoff, so it never replaces the selected block. Typing filters and focuses the first label or friendly alias match so Enter applies it: ordinary prose is named **Paragraph**, while queries such as `p`, `text`, `h2`, and `heading 2` resolve consistently in Change type and slash creation. Backspace on the initially empty search still deletes the selected blocks, while Backspace edits the query after typing begins. The selected blocks convert among paragraph, heading levels, bullet/number/task lists, quote, code, and divider while preserving useful text. Changing among bullet, numbered, and task types retains each item's indentation. Converting one multiline paragraph to a bullet, numbered, or task list creates one list item per visible line and retains each line's inline marks. Converting a single-column table creates one list item per row and retains in-cell marks and line breaks. Conversion then clears block selection and places the cursor at the end of the last converted block. |
| Focus on one section | Toggle a heading caret, press Enter at the end of a collapsed heading, or click below a final collapsed section | Expanded headings reveal the caret only over the caret's own hit target; collapsed headings keep it visible. Blocks and their external hover controls until the next equal-or-higher heading or divider hide or reappear immediately, without a layout transition or residual rich-block box. Enter at a collapsed heading's end keeps its existing section hidden, inserts or reuses a divider after that section, and focuses a visible blank paragraph below the divider, including when selection remained inside a newly hidden code block. Clicking below the document uses the same exit when the final visible block is a collapsed heading even though hidden descendants are the stored final nodes. Otherwise that click focuses an existing final blank paragraph or appends and focuses a normal paragraph without a divider. Expanded-heading Enter retains normal split behavior. The divider persists as `---` and is excluded from collapsed-section visibility and drag/drop ranges; collapse state itself remains presentation-only. |
| Work with tables | Put the cursor in a table or scroll the document around it | Table row/column controls are hidden while their interaction is redesigned. Tab and Shift-Tab still navigate cells and column resizing remains available. A Markdown table stays in the editor content flow and scrolls only with the surrounding page; it does not own a nested scroll container. Selected cells and whole selected tables use the same blue highlight as other selected blocks. |
| Browse database table views | Open a database page or an embedded database table | Full-page and embedded databases use one shared database component; its embed variation only adds the source link/dropdown. Tabs occupy the same transparent toolbar row as search/filter/source/new controls, with embedded source between Filter and New. The fixed 48-pixel toolbar uses 4-pixel vertical padding around a 40-pixel content band that centers every tab and control on one Y-axis, followed by an additional 6-pixel gap before the heading. Multiple table views retain independent nested filters, sorts, and exact visible columns. Views appear as 40-pixel independent pills: the active pill is filled and uses stronger text, while inactive pills have a neutral outline. Search, Filter, New, and the borderless Add View control retain their compact button sizes and remain centered; Add View is transparent until hover gives it a neutral fill. Primary-modifier click on **New** projects the canonical created record into the current sidebar tree, opens it, and selects its default title for replacement immediately after the create response, before any record-page hydration or table reload; ordinary click keeps the record in the table's inline name editor. Pills do not overlap or join the table border. The theme-background table heading uses only top and bottom rules. Compact, borderless bulk-selection checkboxes appear instantly on the block-handle axis outside the table surface, remain fixed during horizontal scrolling, and stay interactive inside embeds. A measured sibling overlay gives each checkbox a transparent hover target spanning the full height of its real header or row and the full gutter width to the table edge; the table contains no checkbox cells or anchors, so its normal-width scroll frame remains exactly constrained to the container. Tables use the editor content width and scroll oversized rows or columns inside a shared two-axis frame. Cell values wrap by default. Record names become plain inline editors with no focus border, background change, row-height shift, or text-baseline movement, and the caret starts at the end. Full-height header dividers appear only on header hover/focus to identify resizable boundaries; widths are browser-local preferences scoped to the workspace, database, and stable view ID rather than shared schema data. Table search expands immediately to a 224-pixel theme-background surface that fills the content band, so its inactive-pill-colored bottom rule exactly aligns with the pill bottoms. A separate preceding 44-pixel theme-background gradient runs from zero opacity through 50% opacity at 30% of its width to fully opaque, making pill overlap fade earlier and more clearly. Bulk mode reuses that band and fade, with every label sharing the toolbar's vertical center. The selection count becomes a Sky 500 underlined `Clear all` action on hover/focus. Embedded bulk mode omits the database source selector. Escape closes and clears search and clears bulk selection, including both latent states when selection has hidden an open search. The frame has no outer border, top/bottom section separators, final-row bottom rule, or manual Refresh control; loading remains application-owned. |
| Filter database records | Activate the filter icon beside New on a database page or embed, build the rules, and select Apply | An anchored filter builder restricts conditions and values to the selected property type, offers searchable select-option values, and supports nested groups with isolated `and`/`or` logic. All edits remain local until Apply saves the complete tree to the active `.db.md` view for runtime evaluation. |
| Control database property visibility | Use a table column header context menu or an individual record page's properties menu | A column can be hidden and hidden columns can be restored only through table header actions; there is no toolbar visibility control. Table columns are shown or hidden only in the active view. Record-page properties use independent database-level record-page settings. Neither action deletes schema or frontmatter values. |
| Create a property | Activate a table or page property-create trigger, type a name, choose a type, and confirm | An anchored shared menu uses its search field as the exact name and type-ahead hint, displays types as an icon grid, supports arrow navigation, defaults to Text, moves focus to Date for a prefix such as `Dat`, confirms type on the first Enter, and creates on the second Enter. |
| Read and reuse code | Use a code block toolbar | The language is stored in the fence and selected through a searchable styled menu rather than a native browser selector, and source can be copied. Code blocks, Mermaid edit mode and error messages, inline code, and the application address bar resolve their background from one system-wide `surface-subtle` token set to Neutral 100 in Light and Neutral 700 in Dark; Mermaid source is fixed to Mermaid without a language selector. Mermaid defaults to a borderless theme-aware diagram view whose absolute top-right view/edit switcher appears on hover. Theme changes re-render open diagrams with Mermaid's matching Light or Dark theme. Moving a caret into it, including after deleting the previous block, reveals source at the diagram view's captured height without vertical overflow or scrolling; moving that caret outside its source returns only that Mermaid block to view, while range-selection gestures retain native editing behavior. Within code or Mermaid source, Mod-Up/Down first moves to that block's boundary; from there, the next press retains the platform's document-boundary behavior. |
| Reuse content outside Rumi | Copy a text or block selection into a rich or plain destination | The browser clipboard carries every selected block in document order as portable semantic `text/html` plus readable `text/plain`, even when Rumi's explicit block highlight is represented internally by only one native selected node or an empty text cursor. Rich destinations such as Google Docs and Slack receive the complete selection with common formatting. Code uses semantic `<pre><code>` plus a neutral monospace/background presentation so rich editors that flatten the elements can retain a recoverable code boundary. Plain destinations retain line, paragraph, list, task, table, code, and embed boundaries. Rumi also carries the same complete selection as a private exact slice for lossless Rumi-to-Rumi paste. Cutting an explicit block selection exports and removes the same complete set. |
| Reuse workspace assets | Insert, paste, drop, resize, or align an image, static SVG, or PDF | The client uploads bytes through the API; the runtime chooses a collision-safe `.assets/` path and emits `asset.changed`; Markdown stores only the relative path. Images default left. An image resize previews locally during the drag and persists once on release; left, center, and full alignment actions persist immediately beside the width as shared typed state under `.rumi/`. Both are applied before image rendering for every client without changing Markdown/YAML. The visible resize handle is 6 pixels wide with an expanded invisible pointer target. Repeated occurrences of one image source on a page share the same width and alignment. SVG must be well-formed and static: scripts, event handlers, animation, embedded documents, external resource references, and other active content are rejected. Rumi-controlled and uniquely fingerprinted external asset renames repair matching Markdown image and file destinations in the background while preserving labels, fragments, code examples, and matching shared presentation. |
| Manage Trash | Open Trash from the sidebar, then open, restore, or permanently delete an item | `/trash` uses the ordinary editor page title and content container without a title icon. Item kinds use a small unboxed icon beside the Page, Folder, Database, Upload, or File label rather than a leading item icon. Restore and Delete are ghost actions; destructive hover emphasis uses Rose 600. The address bar keeps the ordinary editor-page maximum width. |
| See linked resources at block level | Use standalone URLs, `![[file]]`, images, or `db` fences | The editor renders bookmark, file, image, and database-reference NodeViews while round-tripping the original Markdown syntax. |

## Migration Boundaries

- List items temporarily retain the proven editor's flat `indent` attribute model. This preserves
  per-item handles and the established drag behavior: vertical position chooses a block gap, while
  moving right chooses an allowed indent. Serialization still produces nested Markdown lists.
- Task items accept standard spaced GFM markers and legacy compact unchecked markers at every
  supported nesting depth. The editor writes the portable canonical source forms `- [ ]` and
  `- [x]`. Empty task source lines have no trailing space.
- Handles use one fixed editor-gutter X-axis even when list content is indented. The editor and the
  gutter to its left both activate the corresponding block handle.
- Heading collapse mode and Mermaid view mode are presentation state, not document data.
- The editor does not call filesystem or Electron APIs. Workspace assets go through runtime/API
  commands, so other clients can implement the same behavior.
- Automated platform-boundary coverage rejects Electron package imports, preload/IPC bridge APIs,
  legacy renderer imports, and Electron runtime dependencies from the active apps and packages.
- Bookmark metadata fetching is not required for a bookmark to work. The durable value is the URL.
- Mermaid uses the regular Phosphor Flow Arrow icon in the canonical block-type presentation shared
  by the handle context menu and slash menu.
- Database embeds remain references to database sources. Database relations are deliberately not
  implemented by this contract; Decision 019 remains the separate schema decision.
- A database embed may persist its selected stable view ID in the existing `db` fence. Saved view
  configuration still belongs only to the source database's `.db.md` file.

## Persistence Boundary

Every document-changing action updates ProseMirror first. Serialization is deferred from the
keystroke path and the client requests the current draft for autosave. Markdown/frontmatter remain
canonical, while selection, open menus, collapsed headings, and Mermaid display mode remain client
state. Inline replacement glyphs and selected emoji are real ProseMirror text, so the normal dirty,
serialization, versioned-save, revision, and reconciliation path persists them without a parallel
filesystem write.
